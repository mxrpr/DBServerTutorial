package com.mixer.query.sqltokens;

import java.util.Vector;

/**
 * Class to represent the SQL 'or' keyword
 */
public class SQLValues extends SQLWhere {
    public SQLValues(SQLTYPE type) {
        super(type);
    }
}